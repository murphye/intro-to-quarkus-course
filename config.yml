title: Intro to Quarkus
description: Get started using Quarkus
template:
  repo: intro-to-quarkus-template
  name: intro-to-quarkus
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
